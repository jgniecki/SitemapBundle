# src/Resources/config/routes/sitemap.yaml
# Register routes generated by the SitemapBundle.
# This file is copied to your application's config/routes directory when
# running "php bin/console sitemap:install".

sitemap:
    resource: '.'
    type: sitemap
